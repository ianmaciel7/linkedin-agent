# Generated Artifact Rules

Keep generated agent integration artifacts separate from hand-written policy.

Do not edit generated artifacts manually when the upstream workflow provides a
refresh command.

For OpenSpec-generated agent integrations, refresh them with `openspec update`
instead of hand-editing the generated files.

When the repository uses skill sync metadata, update lock or sync artifacts
through the supported workflow rather than ad hoc manual edits.
